What's new

1.2.9
Organize is locked to its content height. Settings (Beta) can still be stretched taller.

1.2.8
The native window uses the editor skin color so the first frame is not white.

1.2.7
The window opens at the Organize size without the white flash.

1.2.6
The window can grow taller. The default height still fits the Organize tab.
Settings stays inside that height instead of expanding the window.
Opening on Settings uses the Organize tab size so the window does not flash and grow.

1.2.5
Settings folders show as parents and children, with the full path under each child.
Settings shows the orange special-use warning above Shader, MonoScript, and DefaultAsset.
